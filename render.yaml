services:
  - type: web
    name: ifsc
    env: python
    plan: free
    autoDeploy: true
    region: singapore
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.rbi_api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health